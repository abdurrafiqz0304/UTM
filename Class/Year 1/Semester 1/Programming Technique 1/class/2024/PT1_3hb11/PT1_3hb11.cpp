#include <iostream>
using namespace std;

int main(){

    int age;
    if (age < 18){
        cout << "You can't vote";
    } else if (age < 66){
        cout << "Allowed without helper";
    } else (age > 65); {
        cout << "Allowed with helper";
    }
    
    
    

    system("pause");
    return 0;
}